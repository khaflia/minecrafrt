package me.kbk.dhabicore.listeners;

import me.kbk.dhabicore.DhabiCore;
import org.bukkit.event.Listener;

// Placeholder - TPA logic is handled in TpaManager and commands
// Home logic is handled in HomeCommand and GuiListener
public class TpaListener implements Listener {
    public TpaListener(DhabiCore plugin) {}
}
